[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "a-share-lgbm-75-25-strategy"
version = "0.2.0"
description = "Frozen 75/25 Alpha158-Barra LightGBM with a causal volatility-target risk overlay"
requires-python = ">=3.9"
dependencies = [
  "akshare>=1.16.0",
  "apscheduler>=3.10,<4",
  "baostock>=0.9.3,<1",
  "fastapi>=0.115,<1",
  "httpx>=0.27,<1",
  "jinja2>=3.1,<4",
  "numpy>=1.24,<3",
  "pandas>=2.0,<3",
  "python-dotenv>=1.0,<2",
  "uvicorn[standard]>=0.30,<1",
]

[project.optional-dependencies]
dev = ["pytest>=8,<9"]
research = ["lightgbm==4.6.0"]

[tool.setuptools.packages.find]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
